package HomeWork4;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.time.temporal.ValueRange;

import javax.swing.plaf.basic.BasicInternalFrameTitlePane.MaximizeAction;

import org.omg.CosNaming.NamingContextPackage.NotFound;

public class Main {

	public static void main(String[] args) throws IOException {
		
		/*
		//HomeWork(a)
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		
		int c = 0;
		
		System.out.println("Enter first number:");
		float first = Float.parseFloat(br.readLine());
		
		System.out.println("Enter second number:");
		float second = Float.parseFloat(br.readLine());
		
		System.out.println("Enter third number:");
		float third = Float.parseFloat(br.readLine());
		
		
						//ValueRange range = ValueRange.of(5,-5);// Запитатись за range не хоче конвертувати float в long//    
		
		if(first >= -5 && first <=5){
			c++;
		}
		if(second >= -5 && second <=5){
			c++;
		}
		if(third >= -5 && third <=5){
			c++;
		}
		
		System.out.println(c + " float number(s) belong to the range");
		*/
		
		
		
		
		
		/*
		 //HomeWork(b)
		  * 
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		
		System.out.println("Enter first number:");
		int first = Integer.parseInt(br.readLine());
		
		System.out.println("Enter second number:");
		int second = Integer.parseInt(br.readLine());
		
		System.out.println("Enter third number:");
		int third = Integer.parseInt(br.readLine());
										
		int maxValue = (first >= second && first >= third) ? first : (second >= third && second >= first) ? second : third;
		System.out.println("Max value " + maxValue);

		int minValue = (first <= second && first <= third) ? first : (second <= third && second <= first) ? second : third;
		System.out.println("Min value " + minValue);
		*/		
		
		
		
		/*
		// HomeWork(c)
		
		BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
		
		System.out.println("Enter the number of HTTPerror");
		int error = Integer.parseInt(br.readLine());
		
		switch(error){
		case 404: System.out.println("The HTTP error: " + HttpError.NotFound404);
		break;
		case 405: System.out.println("The HTTP error: " + HttpError.MethodNotAllowed405);
		break;
		case 406: System.out.println("The HTTP error: " + HttpError.NotAcceptable406);
		break;
	}
		*/
		
		
		
		
		
		
		
		
		
		
		
		
		
	}

}
